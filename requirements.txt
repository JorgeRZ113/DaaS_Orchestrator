# Runtime dependencies (aligned with pyproject.toml)
fastapi
uvicorn[standard]
pydantic
pyyaml
python-dotenv
httpx
# La exige Starlette para `request.form()`: subida multipart del descriptor.
python-multipart
